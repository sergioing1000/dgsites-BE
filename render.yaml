services:
  - type: web
    name: wind-data-api
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn main.py:app --host 0.0.0.0 --port 10000
    envVars:
      - key: ENVIRONMENT
        value: production
