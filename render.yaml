services:
  - type: web
    name: wind-data-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m uvicorn main:app --port $PORT"
    envVars:
      - key: ENVIRONMENT
        value: production
